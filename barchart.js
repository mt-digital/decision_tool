/**
 * Plot bar charts comparing the region selected in add_counties.js to the 
 * full sample region.
 */

// set up standard margins 

var margin = {top: 20, right: 

var svg = d3.select("body").append("svg")

d3.json( "example.json", function (error, data) {
    svg.select("    
        

}
